import { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { BadgeCheck, Bot, CheckCircle2, Circle, CircleX, Maximize, Plus, Sparkles, Trash2, ZoomIn, ZoomOut } from 'lucide-react'
import { ApiError, api } from '../lib/api'
import { t } from '../lib/i18n'
import { parseSse } from '../lib/sse'
import { getRunEvents, listRuns, resumeGate, startRun, validateWorkflow, type RunEvent, type RunInput, type RunState } from '../lib/runsApi'
import GateCard from '../components/GateCard'
import RunInputPicker from '../components/RunInputPicker'
import { Button, IconButton, Input, Select, Textarea } from '../lib/ui'
import { resolveProvider } from '../lib/uiHelpers'

type Agent = { id: string; name?: string; provider?: string; [key: string]: unknown }
type AgentNode = { id: string; agent: string; prompt: string; gate: 'none' | 'approval'; type?: 'agent'; [key: string]: unknown }
type ValidateNode = { id: string; type: 'validate'; target: string; checks: Array<Record<string, unknown>>; on_fail: 'interrupt' | 'fail'; [key: string]: unknown }
type Node = AgentNode | ValidateNode
type EdgeKind = 'default' | 'success' | 'warning' | 'error'
type Edge = [string, string] | { from: string; to: string; kind?: EdgeKind; label?: string }
type Workflow = { id: string; nodes: Node[]; edges: Edge[]; stop: Record<string, unknown>; [key: string]: unknown }
type Layout = Record<string, { x: number; y: number }>
type Log = RunEvent & { ts: string }
const isValidate = (node: Node): node is ValidateNode => node.type === 'validate'
const edgeFrom = (edge: Edge) => Array.isArray(edge) ? edge[0] : edge.from
const edgeTo = (edge: Edge) => Array.isArray(edge) ? edge[1] : edge.to
const edgeKind = (edge: Edge): EdgeKind => Array.isArray(edge) ? 'default' : edge.kind ?? 'default'
const edgeLabel = (edge: Edge) => Array.isArray(edge) ? '' : edge.label ?? ''
const edgeKey = (edge: Edge) => `${edgeFrom(edge)}→${edgeTo(edge)}`
const edgeColor = (kind: EdgeKind) => kind === 'success' ? 'var(--hub-success)' : kind === 'warning' ? 'var(--hub-warning)' : kind === 'error' ? 'var(--hub-error)' : 'var(--hub-edge-normal)'
// Must equal the node card's real rendered height (content is single-line/
// truncated throughout, so this is constant regardless of node type or text).
// A mismatch here desyncs edge endpoints, the arrow marker and addNode's drop
// offset from the actual DOM — which is exactly what made arrowheads land
// off the port before (a stale 126 vs the card's real 107px).
const size = { width: 220, height: 107 }
const meta = (run: RunState) => run.metadata ?? {}
const nextId = (nodes: Node[]) => { let i = 1; const ids = new Set(nodes.map(node => node.id)); while (ids.has(`node-${i}`)) i++; return `node-${i}` }
const layoutFor = (nodes: Node[], saved: Layout): Layout => Object.fromEntries(nodes.map((node, i) => [node.id, saved[node.id] ?? { x: 70 + i * 260, y: 90 }]))
function chain(workflow: Workflow) { const incoming = new Set(workflow.edges.map(edgeTo)); const byId = new Map(workflow.nodes.map(node => [node.id, node])); const start = workflow.nodes.find(node => !incoming.has(node.id)); if (!start) return workflow.nodes; const next = new Map(workflow.edges.map(edge => [edgeFrom(edge), edgeTo(edge)])); const result: Node[] = []; const seen = new Set<string>(); let current: Node | undefined = start; while (current && !seen.has(current.id)) { result.push(current); seen.add(current.id); current = byId.get(next.get(current.id) ?? '') } return result.length === workflow.nodes.length ? result : workflow.nodes }
function isLinearChain(workflow: Workflow) { if (workflow.nodes.length < 2) return workflow.edges.length === 0; if (workflow.edges.length !== workflow.nodes.length - 1) return false; const incoming = new Map<string, number>(); const outgoing = new Map<string, number>(); for (const edge of workflow.edges) { const from = edgeFrom(edge), to = edgeTo(edge); if (!workflow.nodes.some(node => node.id === from) || !workflow.nodes.some(node => node.id === to) || from === to) return false; incoming.set(to, (incoming.get(to) ?? 0) + 1); outgoing.set(from, (outgoing.get(from) ?? 0) + 1) } if ([...incoming.values(), ...outgoing.values()].some(count => count > 1)) return false; const start = workflow.nodes.find(node => !incoming.has(node.id)); if (!start) return false; const visited = new Set<string>(); let current: string | undefined = start.id; while (current && !visited.has(current)) { visited.add(current); const following = workflow.edges.find(edge => edgeFrom(edge) === current); current = following ? edgeTo(following) : undefined } return visited.size === workflow.nodes.length }
function refErrors(nodes: Node[]) { const positions = new Map(nodes.map((node, i) => [node.id, i])); const result = new Map<string, string[]>(); nodes.forEach((node, i) => { const found: string[] = []; if (isValidate(node)) { const p = positions.get(node.target); if (p === undefined || p >= i) found.push(`target ${node.target}`) } else for (const match of node.prompt.matchAll(/\{\{(.*?)\}\}/g)) { const token = match[1].trim(); const ref = token.match(/^(.+)_(output|claims)$/); const p = ref ? positions.get(ref[1]) : undefined; if (token !== 'objective' && token !== 'inputs' && (!ref || p === undefined || p >= i)) found.push(token) } if (found.length) result.set(node.id, found) }); return result }
function scalar(value: unknown) { return typeof value === 'string' ? JSON.stringify(value) : JSON.stringify(value) }
function yaml(value: unknown, indent = 0): string[] { const pad = ' '.repeat(indent); if (Array.isArray(value)) return value.flatMap(item => item && typeof item === 'object' ? [`${pad}-`, ...yaml(item, indent + 2)] : [`${pad}- ${scalar(item)}`]); if (value && typeof value === 'object') return Object.entries(value as Record<string, unknown>).flatMap(([key, item]) => item && typeof item === 'object' ? [`${pad}${key}:`, ...yaml(item, indent + 2)] : [`${pad}${key}: ${scalar(item)}`]); return [`${pad}${scalar(value)}`] }
const toYaml = (workflow: Workflow) => `${yaml(workflow).join('\n')}\n`

export default function WorkflowsPage() {
  const [items, setItems] = useState<Workflow[]>([]); const [selected, setSelected] = useState<Workflow | null>(null); const [agents, setAgents] = useState<Agent[]>([]); const [classes, setClasses] = useState<Record<string, { provider: string }>>({}); const [layout, setLayout] = useState<Layout>({}); const layoutRef = useRef<Layout>({}); const [activeId, setActiveId] = useState<string | null>(null); const [query, setQuery] = useState(''); const [newId, setNewId] = useState<string | null>(null); const [pendingDelete, setPendingDelete] = useState<string | null>(null); const [dirty, setDirty] = useState(false); const [notice, setNotice] = useState(''); const [errors, setErrors] = useState<string[]>([]); const [checksText, setChecksText] = useState(''); const [checksError, setChecksError] = useState(''); const [tab, setTab] = useState<'overview'|'contracts'|'runs'|'alerts'>('overview'); const [inspector, setInspector] = useState(true); const [zoom, setZoom] = useState(1); const [pan, setPan] = useState({ x: 0, y: 0 }); const [edge, setEdge] = useState<string | null>(null); const [draftEdge, setDraftEdge] = useState<string | null>(null); const [objective, setObjective] = useState(''); const [inputs, setInputs] = useState<RunInput[]>([]); const [run, setRun] = useState<RunState | null>(null); const [logs, setLogs] = useState<Log[]>([]); const [runs, setRuns] = useState<RunState[]>([]); const [eventMap, setEventMap] = useState<Record<string, RunEvent[]>>({}); const [busy, setBusy] = useState(false); const [stopping, setStopping] = useState(false); const [resuming, setResuming] = useState(false); const drag = useRef<{ id: string; dx: number; dy: number } | null>(null); const panDrag = useRef<{ x: number; y: number; px: number; py: number } | null>(null); const controller = useRef<AbortController | null>(null); const canvasRef = useRef<HTMLDivElement>(null)
  const ordered = useMemo(() => selected ? chain(selected) : [], [selected]); const refs = useMemo(() => refErrors(ordered), [ordered]); const active = selected?.nodes.find(node => node.id === activeId); const filtered = items.filter(item => item.id.toLowerCase().includes(query.trim().toLowerCase())); const providers = [...new Set((selected?.nodes ?? []).filter(node => !isValidate(node)).map(node => agents.find(agent => agent.id === node.agent)?.provider).filter((provider): provider is string => Boolean(provider)).map(provider => resolveProvider(provider, classes)))]; const linear = selected ? isLinearChain(selected) : true; const interrupt = run?.interrupts?.find(item => item.status === 'pending') ?? null; const relevantRuns = active ? runs.filter(row => String(meta(row).workflow_id ?? '') === selected?.id) : []; const alerts = active ? relevantRuns.flatMap(row => (eventMap[row.run_id] ?? []).filter(event => event.node === active.id && (event.type === 'error' || event.type === 'validation_fail'))) : []
  useEffect(() => { void Promise.all([api<Workflow[]>('/api/workflows'), api<Agent[]>('/api/agents'), api<Record<string, { provider: string }>>('/api/model-classes'), listRuns()]).then(([rows, listedAgents, modelClasses, recent]) => { setItems(rows); setSelected(rows[0] ?? null); setAgents(listedAgents); setClasses(modelClasses); setRuns(recent) }).catch(error => setNotice(error instanceof ApiError ? error.message : t('workflows.loadFailed'))) }, [])
  const selectedRef = useRef(selected); selectedRef.current = selected; const selectedId = selected?.id ?? null
  useEffect(() => { setPendingDelete(null) }, [selectedId])
  // Keyed on the workflow id, not the workflow object. update() replaces that
  // object on every edit, so depending on it re-ran this after each change:
  // server layout overwrote positions of nodes just dropped, fit() reset the
  // canvas mid-gesture (which is why drag-to-connect failed right after a
  // delete), and setDirty(false) here undid the setDirty(true) from the edit,
  // so the unsaved marker never appeared.
  useEffect(() => { const workflow = selectedRef.current; if (!workflow) return; setActiveId(null); setDirty(false); void api<{ nodes: Layout }>(`/api/workflows/${encodeURIComponent(workflow.id)}/layout`).then(data => { const next = layoutFor(workflow.nodes, data.nodes); layoutRef.current = next; setLayout(next) }).catch(() => { const next = layoutFor(workflow.nodes, {}); layoutRef.current = next; setLayout(next) }).finally(() => requestAnimationFrame(fit)) }, [selectedId])
  useEffect(() => { if (active && isValidate(active)) setChecksText(JSON.stringify(active.checks, null, 2)); else setChecksText('') }, [active])
  useEffect(() => () => controller.current?.abort(), [])
  useEffect(() => { if (!selected || (tab !== 'runs' && tab !== 'alerts')) return; const rows = runs.filter(row => String(meta(row).workflow_id ?? '') === selected.id); void Promise.all(rows.map(row => getRunEvents(row.run_id).then(events => [row.run_id, events] as const).catch(() => [row.run_id, []] as const))).then(entries => setEventMap(Object.fromEntries(entries))) }, [selected, runs, tab])
  const update = (fn: (workflow: Workflow) => Workflow) => { setSelected(current => current ? fn(current) : current); setDirty(true); setNotice(''); setErrors([]) }
  const validate = async () => { if (!selected) return false; const result = await validateWorkflow({ yaml_text: toYaml(selected) }); setErrors(result.errors); setNotice(result.ok ? t('workflows.validateSuccess') : t('workflows.validationBackendError')); return result.ok }
  const save = async () => { if (!selected || checksError || !(await validate())) return false; try { const result = await api<Workflow>(`/api/workflows/${encodeURIComponent(selected.id)}/model`, { method: 'PUT', body: JSON.stringify({ model: selected }) }); await api(`/api/workflows/${encodeURIComponent(selected.id)}/layout`, { method: 'PUT', body: JSON.stringify({ nodes: layoutRef.current }) }); setSelected(result); setItems(rows => rows.map(row => row.id === result.id ? result : row)); setDirty(false); setNotice(t('workflows.saved')); return true } catch (error) { setNotice(error instanceof ApiError ? error.message : t('workflows.saveFailed')); return false } }
  const addNode = (agentId: string, point: { x: number; y: number }, type: 'agent'|'validate') => { if (!selected) return; const id = nextId(selected.nodes); const previous = ordered.at(-1); const node: Node = type === 'validate' ? { id, type, target: previous?.id ?? '', checks: [], on_fail: 'interrupt' } : { id, agent: agentId, prompt: previous ? `{{${previous.id}_output}}` : '{{objective}}', gate: 'none' }; update(workflow => ({ ...workflow, nodes: [...workflow.nodes, node], edges: previous ? [...workflow.edges, [previous.id, id]] : workflow.edges })); const next = { ...layoutRef.current, [id]: { x: Math.max(0, (point.x - pan.x) / zoom - 110), y: Math.max(0, (point.y - pan.y) / zoom - size.height / 2) } }; layoutRef.current = next; setLayout(next); setActiveId(id) }
  const connect = (from: string, to: string) => { if (from !== to) update(workflow => ({ ...workflow, edges: [...workflow.edges, [from, to]] })) }
  const deleteEdge = (key: string) => { if (!selected) return; update(workflow => ({ ...workflow, edges: workflow.edges.filter(item => edgeKey(item) !== key) })); setEdge(null) }
  const removeEdge = () => { if (edge) deleteEdge(edge) }
  const saveLayout = () => { if (selected) void api(`/api/workflows/${encodeURIComponent(selected.id)}/layout`, { method: 'PUT', body: JSON.stringify({ nodes: layoutRef.current }) }).catch(() => undefined) }
  const stream = useCallback(async (response: Response) => { if (!response.body) throw new Error('Run stream unavailable'); for await (const item of parseSse(response.body)) { const data = item.data as Record<string, unknown>; setLogs(current => [{ ...data, type: item.event, ts: new Date().toISOString() } as Log, ...current].slice(0, 80)); if (item.event === 'state_snapshot' && data.state && typeof data.state === 'object') setRun(data.state as RunState); if (item.event === 'interrupt') setRun(current => current ? { ...current, status: 'interrupted', interrupts: [...(current.interrupts ?? []), data] } : current); if (item.event === 'done') setRun(current => current ? { ...current, status: String(data.status ?? current.status) } : current); if (item.event === 'error') setNotice(String(data.message ?? t('workflows.runFailed'))) } }, [])
  const start = async () => { if (!selected || busy) return; if (interrupt) { setNotice(t('workflows.resolvePendingGate')); return } if (!objective.trim()) { setNotice(t('workflows.enterObjective')); return } setBusy(true); setStopping(false); setLogs([]); setRun(null); try { if (dirty && !(await save())) return; const result = await validateWorkflow({ yaml_text: toYaml(selected) }); if (!result.ok) { setErrors(result.errors); setNotice(t('workflows.cannotRunInvalid')); return } controller.current = new AbortController(); await stream(await startRun(selected.id, objective.trim(), inputs, controller.current.signal)); void listRuns().then(setRuns).catch(() => undefined) } catch (error) { if ((error as Error).name !== 'AbortError') setNotice(error instanceof Error ? error.message : t('workflows.runFailed')) } finally { setBusy(false); setStopping(false); controller.current = null } }
  const stop = () => { if (!controller.current || stopping) return; setStopping(true); setNotice(t('workflows.stopping')); controller.current.abort() }
  const gate = async (approved: boolean) => { if (!run || !interrupt) return; setResuming(true); try { await stream(await resumeGate(run.run_id, String(interrupt.interrupt_id), approved)) } catch (error) { setNotice(error instanceof Error ? error.message : t('workflows.cannotResumeRun')) } finally { setResuming(false) } }
  const nodeState = (id: string) => { const state = run ? meta(run) : {}; if (Array.isArray(state.completed_nodes) && state.completed_nodes.map(String).includes(id)) return 'done'; if (String(state.current_node ?? '') === id && busy) return 'running'; if (String(state.current_node ?? '') === id && run?.status === 'failed') return 'fail'; return 'idle' }
  // Single source of truth for screen→world conversion. Node drag used to mix
  // screen-pixel deltas with world-unit offsets (dividing by zoom twice in effect),
  // which drifted the node away from the cursor at any zoom other than 1 — worse
  // right after "Fit canvas", which almost never lands on exactly 100%.
  const toWorld = (clientX: number, clientY: number) => { const rect = canvasRef.current?.getBoundingClientRect(); if (!rect) return { x: 0, y: 0 }; return { x: (clientX - rect.left - pan.x) / zoom, y: (clientY - rect.top - pan.y) / zoom } }
  const panStart = (event: React.PointerEvent) => { if (event.target === event.currentTarget) panDrag.current = { x: pan.x, y: pan.y, px: event.clientX, py: event.clientY } }
  const pointerMove = (event: React.PointerEvent) => { if (drag.current) { const p = drag.current; const cursor = toWorld(event.clientX, event.clientY); const next = { ...layoutRef.current, [p.id]: { x: Math.max(0, cursor.x - p.dx), y: Math.max(0, cursor.y - p.dy) } }; layoutRef.current = next; setLayout(next) } else if (panDrag.current) { const p = panDrag.current; setPan({ x: p.x + event.clientX - p.px, y: p.y + event.clientY - p.py }) } }
  const pointerUp = () => { if (drag.current) saveLayout(); drag.current = null; panDrag.current = null }
  const nudgeNode = (id: string, dx: number, dy: number) => { const position = layoutRef.current[id]; if (!position) return; const next = { ...layoutRef.current, [id]: { x: Math.max(0, position.x + dx), y: Math.max(0, position.y + dy) } }; layoutRef.current = next; setLayout(next); setActiveId(id); saveLayout() }
  // Fits the view to the current node bounds, matching a floor of 0.5 so labels
  // stay legible — plain zoom(1)/pan(0,0) would leave nodes laid out below/right
  // of the canvas origin (e.g. code-task's "implement" at x:793,y:406) off-screen.
  const fit = () => {
    const positions = Object.values(layoutRef.current)
    const rect = canvasRef.current?.getBoundingClientRect()
    if (!positions.length || !rect || !rect.width || !rect.height) { setZoom(1); setPan({ x: 0, y: 0 }); return }
    const minX = Math.min(...positions.map(p => p.x)), minY = Math.min(...positions.map(p => p.y))
    const maxX = Math.max(...positions.map(p => p.x + size.width)), maxY = Math.max(...positions.map(p => p.y + size.height))
    const pad = 40
    const next = Math.max(.5, Math.min(1.8, Math.min((rect.width - pad * 2) / (maxX - minX), (rect.height - pad * 2) / (maxY - minY))))
    setZoom(Number(next.toFixed(2)))
    setPan({ x: Math.round((rect.width - (maxX - minX) * next) / 2 - minX * next), y: Math.round((rect.height - (maxY - minY) * next) / 2 - minY * next) })
  }
  // Inline form rather than window.prompt: a native dialog blocks the page and cannot be styled or tested.
  const newWorkflow = async (rawId: string) => { const id = rawId.trim(); if (!id) return; try { setNewId(null); const created = await api<Workflow>('/api/workflows', { method: 'POST', body: JSON.stringify({ id }) }); setItems(rows => [...rows, created]); setSelected(created); setNotice(''); requestAnimationFrame(fit) } catch (error) { setNotice(error instanceof ApiError && error.status === 409 ? t('workflows.alreadyExists') : error instanceof ApiError ? error.message : t('workflows.createFailed')) } }
  // Inline confirmation rather than window.confirm: a native dialog cannot be
  // styled with the rest of the toolbar and blocks the page while open.
  const deleteWorkflow = async () => { if (!selected) return; setPendingDelete(null); try { await api(`/api/workflows/${encodeURIComponent(selected.id)}`, { method: 'DELETE' }); setItems(rows => { const next = rows.filter(row => row.id !== selected.id); setSelected(next[0] ?? null); return next }); setNotice(t('workflows.deleted')) } catch (error) { setNotice(error instanceof ApiError ? error.message : t('workflows.deleteFailed')) } }

  if (!selected) return <div className="p-6 text-sm text-muted">{t('workflows.noValidWorkflow')}</div>
  const selectedEdge = selected.edges.find(item => edgeKey(item) === edge)
  const updateSelectedEdge = (kind: EdgeKind | '', label: string) => { if (!edge) return; update(workflow => ({ ...workflow, edges: workflow.edges.map(item => { if (edgeKey(item) !== edge) return item; const from = edgeFrom(item), to = edgeTo(item); return !kind && !label ? [from, to] : { from, to, ...(kind ? { kind } : {}), ...(label ? { label } : {}) } }) })) }
  return <div className="flex h-full min-h-0 flex-col overflow-hidden bg-app">
    <header className="flex min-h-[var(--hub-size-toolbar)] flex-wrap items-center gap-3 border-b border-border-subtle bg-sidebar px-4 py-2"><div><div className="text-[10px] font-semibold uppercase tracking-[var(--hub-section-tracking)] text-muted">{t('workflows.orchestration')}</div><div className="font-mono text-sm font-semibold">{selected.id}</div></div>{providers.length > 0 && <span className="rounded-full border border-border-subtle px-2 py-1 text-[10px] text-secondary">{t('workflows.providers', { providers: providers.join(', ') })}</span>}<span className="rounded-full border border-border-subtle px-2 py-1 text-[10px] text-muted">{busy ? t('workflows.running') : run?.status ?? t('status.ready')}</span><div className="ml-auto flex flex-wrap items-center gap-2"><Input value={objective} onChange={event => setObjective(event.target.value)} placeholder={t('workflows.objectivePlaceholder')} className="w-44" /><RunInputPicker value={inputs} onChange={setInputs} copy={{ add: t('workflows.addInputs'), artifacts: t('workflows.artifacts'), files: t('workflows.files'), search: t('workflows.searchInputs'), noArtifacts: t('workflows.noArtifacts'), noFiles: t('workflows.noFiles'), artifact: t('workflows.inputArtifact'), file: t('workflows.inputFile'), remove: t('workflows.removeInput') }} />{busy ? <Button variant="destructive" onClick={stop} disabled={!controller.current || stopping}>{stopping ? t('workflows.stoppingLabel') : t('workflows.stop')}</Button> : <Button variant="primary" onClick={() => void start()}>{t('workflows.run')}</Button>}<Button variant="ghost" onClick={() => void validate()}>{t('workflows.validate')}</Button><Button variant="ghost" onClick={() => void save()}>{t('common.save')}</Button>{pendingDelete === selected.id ? <span className="flex items-center gap-2 rounded-md border border-error px-2 py-1"><span className="text-[11px] text-secondary">{t('workflows.deleteConfirm', { id: selected.id })}</span><Button variant="destructive" size="sm" onClick={() => void deleteWorkflow()}>{t('common.delete')}</Button><Button variant="ghost" size="sm" onClick={() => setPendingDelete(null)}>{t('common.cancel')}</Button></span> : <Button variant="destructive" onClick={() => setPendingDelete(selected.id)}><Trash2 className="mr-1" size={16} strokeWidth={1.75} aria-hidden="true" />{t('workflows.deleteWorkflow')}</Button>}<Button variant="ghost" onClick={() => setInspector(value => !value)}>{inspector ? t('workflows.hideInspector') : t('workflows.inspector')}</Button></div></header>
    {(notice || errors.length > 0 || !linear) && <div className={`mx-4 mt-3 rounded-lg border p-3 text-xs ${notice || errors.length > 0 ? 'border-error bg-error-subtle text-error' : 'border-warning bg-warning-subtle text-warning'}`}><div>{notice || (linear ? t('workflows.invalid') : t('workflows.linearOnly'))}</div>{errors.length > 0 && <ul className="mt-2 list-disc space-y-1 pl-5">{errors.map((item, index) => <li key={`${item}-${index}`}>{item}</li>)}</ul>}</div>}
    <div className={`grid min-h-0 flex-1 ${inspector ? 'grid-cols-[248px_minmax(0,1fr)_320px]' : 'grid-cols-[248px_minmax(0,1fr)]'}`}><Sidebar items={filtered} selected={selected} query={query} setQuery={setQuery} onSelect={setSelected} newId={newId} onNewId={setNewId} onCreate={newWorkflow} agents={agents} />
      <main className="flex min-h-0 min-w-0 flex-col"><div className="flex items-center gap-2 border-b border-border-subtle px-4 py-2 text-xs text-secondary"><span>{t('workflows.nodeCount', { count: selected.nodes.length })} · {t('workflows.connectionCount', { count: selected.edges.length })}</span><span>{dirty ? t('workflows.unsaved') : t('workflows.synced')}</span><span className="ml-auto flex items-center gap-1"><IconButton icon={<ZoomIn size={16} strokeWidth={1.75} />} title={t('workflows.zoomIn')} aria-label={t('workflows.zoomIn')} onClick={() => setZoom(value => Math.min(1.8, value + .1))} /><IconButton icon={<ZoomOut size={16} strokeWidth={1.75} />} title={t('workflows.zoomOut')} aria-label={t('workflows.zoomOut')} onClick={() => setZoom(value => Math.max(.5, value - .1))} /><Button variant="ghost" size="sm" onClick={fit}><Maximize className="mr-1" size={16} strokeWidth={1.75} aria-hidden="true" />{t('workflows.fitCanvas')}</Button>{edge && <Button variant="destructive" size="sm" onClick={removeEdge}>{t('workflows.deleteEdge')}</Button>}</span></div><div className="grid min-h-0 flex-1 grid-rows-[minmax(360px,1fr)_minmax(150px,30%)]"><section ref={canvasRef} className="relative min-h-0 overflow-hidden border-b border-border-subtle" onPointerDown={panStart} onPointerMove={pointerMove} onPointerUp={pointerUp} onPointerLeave={pointerUp} onDragOver={event => event.preventDefault()} onDrop={event => { const type = event.dataTransfer.getData('type'); const agent = event.dataTransfer.getData('agent'); const at = toWorld(event.clientX, event.clientY); if (type === 'validate') addNode('', at, 'validate'); else if (agent) addNode(agent, at, 'agent') }} style={{ backgroundImage: 'radial-gradient(var(--hub-border-strong) 1px, transparent 1px)', backgroundSize: '20px 20px' }}><Canvas workflow={selected} layout={layout} zoom={zoom} pan={pan} activeId={activeId} selectedEdge={edge} draftEdge={draftEdge} state={nodeState} onSelect={setActiveId} onDrag={value => { const cursor = toWorld(value.clientX, value.clientY); drag.current = { id: value.id, dx: cursor.x - value.nodeX, dy: cursor.y - value.nodeY } }} onEdgeSelect={setEdge} onEdgeDelete={deleteEdge} onEdgeStart={setDraftEdge} onConnect={connect} onEdgeEnd={() => setDraftEdge(null)} onNudge={nudgeNode} /></section><RunLog logs={logs} run={run} interrupt={interrupt} onGate={gate} resuming={resuming} /></div></main>
      {inspector && <Inspector node={active} edge={selectedEdge} updateEdge={updateSelectedEdge} nodes={ordered} agents={agents} errors={active ? refs.get(active.id) ?? [] : []} tab={tab} setTab={setTab} runs={relevantRuns} alerts={alerts} checksText={checksText} setChecksText={setChecksText} setChecksError={setChecksError} checksError={checksError} update={update} deleteNode={() => { if (!active) return; update(workflow => ({ ...workflow, nodes: workflow.nodes.filter(node => node.id !== active.id), edges: workflow.edges.filter(item => edgeFrom(item) !== active.id && edgeTo(item) !== active.id) })); setActiveId(null) }} />}</div>
  </div>
}

function Sidebar({ items, selected, query, setQuery, onSelect, newId, onNewId, onCreate, agents }: { items: Workflow[]; selected: Workflow; query: string; setQuery: (value: string) => void; onSelect: (workflow: Workflow) => void; newId: string | null; onNewId: (value: string | null) => void; onCreate: (id: string) => void; agents: Agent[] }) {
  // autoFocus alone does not land focus when the field mounts inside an
  // already-rendered sidebar, which left typing going to the document body.
  const newIdRef = useRef<HTMLInputElement>(null); const creating = newId !== null
  useEffect(() => { if (creating) newIdRef.current?.focus() }, [creating])
  return <aside className="flex min-h-0 flex-col overflow-y-auto border-r border-border-subtle bg-sidebar p-3"><div className="mb-3 flex items-center justify-between"><span className="text-[10px] font-semibold uppercase tracking-[var(--hub-section-tracking)] text-muted">{t('nav.workflows')}</span><Button variant="primary" size="sm" onClick={() => onNewId(newId === null ? '' : null)}><Plus className="mr-1" size={16} strokeWidth={1.75} aria-hidden="true" />{t('workflows.new')}</Button></div>{newId !== null && <div className="mb-3 rounded-md border border-border-subtle bg-surface p-2"><Input ref={newIdRef} value={newId} aria-label={t('workflows.newWorkflowPrompt')} placeholder={t('workflows.newIdPlaceholder')} onChange={event => onNewId(event.target.value)} onKeyDown={event => { if (event.key === 'Enter') onCreate(newId); if (event.key === 'Escape') onNewId(null) }} /><div className="mt-2 flex gap-2"><Button variant="primary" size="sm" disabled={!newId.trim()} onClick={() => onCreate(newId)}>{t('workflows.create')}</Button><Button variant="ghost" size="sm" onClick={() => onNewId(null)}>{t('common.cancel')}</Button></div></div>}<Input value={query} onChange={event => setQuery(event.target.value)} placeholder={t('workflows.searchPlaceholder')} className="mb-3" />{items.map(item => <Button key={item.id} variant="ghost" selected={item.id === selected.id} onClick={() => onSelect(item)} className="mb-1 w-full justify-start px-3 py-2 text-left"><span><span className="block font-mono text-xs">{item.id}</span><span className="text-[10px] text-secondary">{t('workflows.nodeCount', { count: item.nodes.length })} · {t('workflows.edgeCount', { count: item.edges.length })}</span></span></Button>)}<div className="mt-4 border-t border-border-subtle pt-3"><div className="mb-2 text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted">{t('workflows.palette')}</div>{agents.map(agent => <div key={agent.id} draggable onDragStart={event => event.dataTransfer.setData('agent', agent.id)} className="mb-2 flex cursor-grab items-center gap-2 rounded-md border border-border-subtle bg-surface px-3 py-2 text-xs"><Bot size={16} strokeWidth={1.75} aria-hidden="true" />{agent.name ?? agent.id}</div>)}<div draggable onDragStart={event => event.dataTransfer.setData('type', 'validate')} className="flex cursor-grab items-center gap-2 rounded-md border border-[var(--hub-node-validate)] bg-surface px-3 py-2 text-xs text-[var(--hub-node-validate)]"><BadgeCheck size={16} strokeWidth={1.75} aria-hidden="true" />validate</div></div></aside> }

function Canvas({ workflow, layout, zoom, pan, activeId, selectedEdge, draftEdge, state, onSelect, onDrag, onEdgeSelect, onEdgeDelete, onEdgeStart, onConnect, onEdgeEnd, onNudge }: { workflow: Workflow; layout: Layout; zoom: number; pan: { x: number; y: number }; activeId: string | null; selectedEdge: string | null; draftEdge: string | null; state: (id: string) => string; onSelect: (id: string) => void; onDrag: (value: { id: string; clientX: number; clientY: number; nodeX: number; nodeY: number }) => void; onEdgeSelect: (edge: string) => void; onEdgeDelete: (edge: string) => void; onEdgeStart: (id: string) => void; onConnect: (from: string, to: string) => void; onEdgeEnd: () => void; onNudge: (id: string, dx: number, dy: number) => void }) {
  const activate = (event: React.KeyboardEvent, action: () => void) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); event.stopPropagation(); action() } }
  return <div className="absolute inset-0" style={{ transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom})`, transformOrigin: '0 0' }}><svg className="pointer-events-none absolute left-0 top-0 h-[1600px] w-[2400px] overflow-visible"><defs>{(['default', 'success', 'warning', 'error'] as EdgeKind[]).map(kind => <marker key={kind} id={`workflow-arrow-${kind}`} markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill={edgeColor(kind)} /></marker>)}</defs>{workflow.edges.map(item => { const from = edgeFrom(item), to = edgeTo(item), kind = edgeKind(item), label = edgeLabel(item); const a = layout[from], b = layout[to]; if (!a || !b) return null; const key = edgeKey(item); const x1 = a.x + size.width, y1 = a.y + size.height / 2, x2 = b.x, y2 = b.y + size.height / 2, cx = (x1 + x2) / 2, cy = (y1 + y2) / 2; const path = `M ${x1} ${y1} C ${cx} ${y1}, ${cx} ${y2}, ${x2} ${y2}`; const color = key === selectedEdge ? 'var(--hub-edge-selected)' : edgeColor(kind); return <g key={key}><path d={path} fill="none" stroke={color} strokeWidth={key === selectedEdge ? 3 : 2} markerEnd={`url(#workflow-arrow-${kind})`} />{label && <text x={cx} y={cy - 8} textAnchor="middle" fill={color} stroke="var(--hub-bg-app)" strokeWidth="4" paintOrder="stroke" className="font-mono text-[10px]">{label}</text>}<path d={path} fill="none" stroke="transparent" strokeWidth="14" role="button" tabIndex={0} aria-label={t('workflows.edgeAria', { from, to })} className="pointer-events-auto cursor-pointer focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent" onPointerDown={event => { event.stopPropagation(); onEdgeSelect(key) }} onFocus={() => onEdgeSelect(key)} onKeyDown={event => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); onEdgeSelect(key) } else if (event.key === 'Delete' || event.key === 'Backspace') { event.preventDefault(); onEdgeDelete(key) } }} /></g> })}</svg>{workflow.nodes.map(node => { const p = layout[node.id] ?? { x: 40, y: 60 }; const status = state(node.id); const nodeKeyDown = (event: React.KeyboardEvent) => { const step = event.shiftKey ? 20 : 10; const move = event.key === 'ArrowLeft' ? [-step, 0] : event.key === 'ArrowRight' ? [step, 0] : event.key === 'ArrowUp' ? [0, -step] : event.key === 'ArrowDown' ? [0, step] : null; if (move) { event.preventDefault(); onNudge(node.id, move[0], move[1]) } else activate(event, () => onSelect(node.id)) }; return <div key={node.id} role="button" tabIndex={0} aria-label={t('workflows.nodeAria', { id: node.id })} style={{ left: p.x, top: p.y, width: size.width }} onFocus={() => onSelect(node.id)} onKeyDown={nodeKeyDown} onPointerDown={event => { if ((event.target as HTMLElement).dataset.port) return; event.stopPropagation(); onDrag({ id: node.id, clientX: event.clientX, clientY: event.clientY, nodeX: p.x, nodeY: p.y }); onSelect(node.id) }} className={`absolute select-none rounded-xl border bg-surface p-3 shadow-lg ${activeId === node.id ? 'border-[var(--hub-accent)] ring-1 ring-[var(--hub-accent)]' : isValidate(node) ? 'border-[var(--hub-node-validate)]' : 'border-border-subtle'} ${status === 'running' ? 'node-pulse border-[var(--hub-accent)]' : ''}`}><div className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-md bg-accent/15 text-[var(--hub-node-agent)]">{isValidate(node) ? <BadgeCheck size={16} strokeWidth={1.75} aria-hidden="true" /> : <Sparkles size={16} strokeWidth={1.75} aria-hidden="true" />}</span><span className="truncate font-mono text-xs font-semibold">{node.id}</span>{!isValidate(node) && node.gate === 'approval' && <span className="ml-auto rounded-full border border-warning px-1 text-[9px] text-warning">approval</span>}</div><div className="mt-3 truncate text-[10px] text-secondary">{isValidate(node) ? `validate · ${node.target || t('workflows.noTarget')}` : node.agent}</div><div className="mt-3 flex justify-between text-[10px] text-muted"><span>{status === 'running' ? t('workflows.running') : status === 'done' ? t('workflows.complete') : status === 'fail' ? t('workflows.failed') : isValidate(node) ? `${node.checks.length} checks` : t('workflows.agent')}</span>{status === 'done' ? <CheckCircle2 size={16} strokeWidth={1.75} aria-hidden="true" /> : status === 'fail' ? <CircleX size={16} strokeWidth={1.75} aria-hidden="true" /> : <Circle size={16} strokeWidth={1.75} aria-hidden="true" />}</div><IconButton data-port="right" icon={<span className="h-2 w-2 rounded-full bg-[var(--hub-edge-normal)]" />} onPointerDown={event => { event.stopPropagation(); onEdgeStart(node.id) }} onKeyDown={event => activate(event, () => onEdgeStart(node.id))} variant="handle" className="absolute -right-2 top-1/2 -translate-y-1/2" aria-label={t('workflows.connectFrom', { id: node.id })} /><IconButton data-port="left" icon={<span className="h-2 w-2 rounded-full bg-[var(--hub-edge-normal)]" />} onPointerUp={event => { event.stopPropagation(); if (draftEdge) onConnect(draftEdge, node.id); onEdgeEnd() }} onKeyDown={event => activate(event, () => { if (draftEdge) onConnect(draftEdge, node.id); onEdgeEnd() })} variant="handle" className="absolute -left-2 top-1/2 -translate-y-1/2" aria-label={t('workflows.connectTo', { id: node.id })} /></div> })}</div>
}

function RunLog({ logs, run, interrupt, onGate, resuming }: { logs: Log[]; run: RunState | null; interrupt: Record<string, unknown> | null; onGate: (approved: boolean) => void; resuming: boolean }) { return <section className="min-h-0 overflow-y-auto bg-surface p-3"><div className="mb-2 flex justify-between text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted"><span>{t('workflows.runLog')}</span><span>{run ? `${run.run_id} · ${run.status}` : t('workflows.noRun')}</span></div>{interrupt && <GateCard interrupt={interrupt} onChoice={onGate} resuming={resuming} />}{logs.length === 0 ? <div className="rounded-md border border-dashed border-border-subtle p-4 text-xs text-muted">{t('workflows.sseLogEmpty')}</div> : logs.map((item, index) => <div key={`${item.ts}-${index}`} className="grid grid-cols-[70px_1fr_auto] gap-3 border-b border-border-subtle py-2 text-[10px]"><span className="font-mono text-muted">{new Date(item.ts).toLocaleTimeString('en-US')}</span><span><b>{String(item.node ?? 'workflow')}</b> · {item.type ?? 'event'} {item.text ? String(item.text).slice(0, 120) : ''}</span><span className={item.type === 'error' || item.type === 'validation_fail' ? 'text-error' : item.type === 'done' ? 'text-success' : 'text-[var(--hub-accent)]'}>{item.type === 'error' || item.type === 'validation_fail' ? 'fail' : item.type === 'done' ? 'pass' : 'live'}</span></div>)}</section> }

function Inspector({ node, edge, updateEdge, nodes, agents, errors, tab, setTab, runs, alerts, checksText, setChecksText, setChecksError, checksError, update, deleteNode }: { node?: Node; edge?: Edge; updateEdge: (kind: EdgeKind | '', label: string) => void; nodes: Node[]; agents: Agent[]; errors: string[]; tab: 'overview'|'contracts'|'runs'|'alerts'; setTab: (tab: 'overview'|'contracts'|'runs'|'alerts') => void; runs: RunState[]; alerts: RunEvent[]; checksText: string; setChecksText: (value: string) => void; setChecksError: (value: string) => void; checksError: string; update: (fn: (workflow: Workflow) => Workflow) => void; deleteNode: () => void }) { if (edge) { const kind = Array.isArray(edge) ? '' : edge.kind ?? ''; const label = edgeLabel(edge); return <aside className="border-l border-border-subtle bg-elevated p-4 text-xs"><div className="mb-4 text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted">{t('workflows.edgeInspector')}</div><div className="mb-4 font-mono text-sm">{edgeFrom(edge)} → {edgeTo(edge)}</div><label className="mb-3 block text-[10px] text-muted">{t('workflows.kind')}<Select value={kind} onChange={event => updateEdge(event.target.value as EdgeKind | '', label)}><option value="">{t('workflows.none')}</option><option value="default">default</option><option value="success">success</option><option value="warning">warning</option><option value="error">error</option></Select></label><label className="block text-[10px] text-muted">{t('workflows.label')}<Input value={label} maxLength={120} onChange={event => updateEdge(kind, event.target.value)} placeholder={t('workflows.optionalDisplayLabel')} /></label></aside> } if (!node) return <aside className="border-l border-border-subtle bg-elevated p-4 text-xs text-muted">{t('workflows.selectNodeOrEdge')}</aside>; const prior = nodes.slice(0, nodes.findIndex(item => item.id === node.id)); const change = (field: string, value: unknown) => update(workflow => ({ ...workflow, nodes: workflow.nodes.map(item => item.id === node.id ? { ...item, [field]: value } as Node : item) })); const tokens = !isValidate(node) ? [...node.prompt.matchAll(/\{\{(.*?)\}\}/g)].map(match => match[1].trim()) : []; const tabs = ['overview','contracts','runs','alerts'] as const; const moveTab = (event: React.KeyboardEvent<HTMLButtonElement>, index: number) => { const next = event.key === 'ArrowRight' ? (index + 1) % tabs.length : event.key === 'ArrowLeft' ? (index + tabs.length - 1) % tabs.length : -1; if (next >= 0) { event.preventDefault(); setTab(tabs[next]); (event.currentTarget.parentElement?.querySelectorAll<HTMLButtonElement>('[role=tab]')[next])?.focus() } }; return <aside className="flex min-h-0 flex-col border-l border-border-subtle bg-elevated"><div className="flex justify-between border-b border-border-subtle p-4"><div><div className="text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted">{t('workflows.inspector')}</div><div className="font-mono text-sm">{node.id}</div></div><Button variant="ghost" size="sm" onClick={deleteNode}>{t('common.delete')}</Button></div><div role="tablist" className="grid grid-cols-4 border-b border-border-subtle px-2">{tabs.map((item, index) => <button key={item} role="tab" aria-selected={tab === item} tabIndex={tab === item ? 0 : -1} onKeyDown={event => moveTab(event, index)} onClick={() => setTab(item)} className={`border-b-2 py-3 text-[10px] ${tab === item ? 'border-[var(--hub-accent)] text-primary' : 'border-transparent text-muted'}`}>{item === 'overview' ? t('workflows.overview') : item === 'contracts' ? t('workflows.contracts') : item === 'runs' ? t('nav.runs') : t('workflows.alerts')}</button>)}</div><div className="min-h-0 flex-1 overflow-y-auto p-4">{tab === 'overview' && <Overview node={node} prior={prior} agents={agents} checksText={checksText} setChecksText={setChecksText} setChecksError={setChecksError} checksError={checksError} change={change} update={update} errors={errors} />}{tab === 'contracts' && <div className="space-y-2 text-xs">{isValidate(node) ? <pre className="overflow-auto rounded-md bg-app p-3 font-mono text-[10px]">{JSON.stringify({ target: node.target, checks: node.checks }, null, 2)}</pre> : tokens.length ? tokens.map(token => <div key={token} className="rounded-md border border-border-subtle bg-surface p-2 font-mono text-[10px]">{`{{${token}}}`} → {token === 'objective' ? t('workflows.runObjective') : token.endsWith('_output') ? t('workflows.outputNode', { id: token.slice(0, -7) }) : t('workflows.claimsNode', { id: token.slice(0, -7) })}</div>) : <span className="text-muted">{t('workflows.noTokens')}</span>}</div>}{tab === 'runs' && <div className="space-y-2 text-[10px]">{runs.length ? runs.map(item => <div key={item.run_id} className="rounded-md border border-border-subtle bg-surface p-2 font-mono">{item.run_id} · {item.status} · {String(meta(item).current_node ?? '')}</div>) : <span className="text-muted">{t('workflows.noRealRuns')}</span>}</div>}{tab === 'alerts' && <div className="space-y-2 text-[10px]">{alerts.length ? alerts.map((item, index) => <div key={`${item.ts}-${index}`} className="rounded-md border border-error bg-error-subtle p-2 text-error">{item.type}: {String(item.message ?? item.target ?? t('workflows.errorEvent'))}</div>) : <span className="text-muted">{t('workflows.noRealAlerts')}</span>}</div>}</div></aside> }
function Overview({ node, prior, agents, checksText, setChecksText, setChecksError, checksError, change, update: _update, errors }: { node: Node; prior: Node[]; agents: Agent[]; checksText: string; setChecksText: (value: string) => void; setChecksError: (value: string) => void; checksError: string; change: (field: string, value: unknown) => void; update: (fn: (workflow: Workflow) => Workflow) => void; errors: string[] }) { return <>{isValidate(node) ? <><label className="mb-3 block text-[10px] text-muted">target<Select value={node.target} onChange={event => change('target', event.target.value)}><option value="">{t('workflows.selectPriorNode')}</option>{prior.map(item => <option key={item.id} value={item.id}>{item.id}</option>)}</Select></label><label className="mb-3 block text-[10px] text-muted">on_fail<Select value={node.on_fail} onChange={event => change('on_fail', event.target.value)}><option value="interrupt">interrupt</option><option value="fail">fail</option></Select></label><label className="block text-[10px] text-muted">checks JSON<Textarea value={checksText} onChange={event => { setChecksText(event.target.value); try { const value = JSON.parse(event.target.value); if (!Array.isArray(value)) throw new Error(); setChecksError(''); change('checks', value) } catch { setChecksError(t('workflows.invalidJson')) } }} rows={8} />{checksError && <span className="text-error">{checksError}</span>}</label></> : <><label className="mb-3 block text-[10px] text-muted">agent<Select value={node.agent} onChange={event => change('agent', event.target.value)}>{agents.map(agent => <option key={agent.id} value={agent.id}>{agent.id}</option>)}</Select></label><label className="mb-3 block text-[10px] text-muted">gate<Select value={node.gate} onChange={event => change('gate', event.target.value)}><option value="none">none</option><option value="approval">approval</option></Select></label><label className="block text-[10px] text-muted">prompt<Textarea value={node.prompt} onChange={event => change('prompt', event.target.value)} rows={8} /></label></>}{errors.length > 0 && <div className="mt-3 rounded-md border border-error bg-error-subtle p-2 text-[10px] text-error">{t('workflows.invalidReference', { errors: errors.join(', ') })}</div>}</> }
