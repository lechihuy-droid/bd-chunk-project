import { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { ApiError, api } from '../lib/api'
import { parseSse } from '../lib/sse'
import { getRunEvents, listRuns, resumeGate, startRun, validateWorkflow, type RunEvent, type RunState } from '../lib/runsApi'
import GateCard from '../components/GateCard'
import { Button, IconButton, Input, Select, Textarea } from '../lib/ui'

type Agent = { id: string; name?: string; provider?: string; [key: string]: unknown }
type AgentNode = { id: string; agent: string; prompt: string; gate: 'none' | 'approval'; type?: 'agent'; [key: string]: unknown }
type ValidateNode = { id: string; type: 'validate'; target: string; checks: Array<Record<string, unknown>>; on_fail: 'interrupt' | 'fail'; [key: string]: unknown }
type Node = AgentNode | ValidateNode
type Workflow = { id: string; nodes: Node[]; edges: Array<[string, string]>; stop: Record<string, unknown>; [key: string]: unknown }
type Layout = Record<string, { x: number; y: number }>
type Log = RunEvent & { ts: string }
const isValidate = (node: Node): node is ValidateNode => node.type === 'validate'
const size = { width: 220, height: 126 }
const meta = (run: RunState) => run.metadata ?? {}
const nextId = (nodes: Node[]) => { let i = 1; const ids = new Set(nodes.map(node => node.id)); while (ids.has(`node-${i}`)) i++; return `node-${i}` }
const layoutFor = (nodes: Node[], saved: Layout): Layout => Object.fromEntries(nodes.map((node, i) => [node.id, saved[node.id] ?? { x: 70 + i * 260, y: 90 }]))
function chain(workflow: Workflow) { const incoming = new Set(workflow.edges.map(edge => edge[1])); const byId = new Map(workflow.nodes.map(node => [node.id, node])); const start = workflow.nodes.find(node => !incoming.has(node.id)); if (!start) return workflow.nodes; const next = new Map(workflow.edges.map(edge => [edge[0], edge[1]])); const result: Node[] = []; const seen = new Set<string>(); let current: Node | undefined = start; while (current && !seen.has(current.id)) { result.push(current); seen.add(current.id); current = byId.get(next.get(current.id) ?? '') } return result.length === workflow.nodes.length ? result : workflow.nodes }
function refErrors(nodes: Node[]) { const positions = new Map(nodes.map((node, i) => [node.id, i])); const result = new Map<string, string[]>(); nodes.forEach((node, i) => { const found: string[] = []; if (isValidate(node)) { const p = positions.get(node.target); if (p === undefined || p >= i) found.push(`target ${node.target}`) } else for (const match of node.prompt.matchAll(/\{\{(.*?)\}\}/g)) { const token = match[1].trim(); const ref = token.match(/^(.+)_(output|claims)$/); const p = ref ? positions.get(ref[1]) : undefined; if (token !== 'objective' && (!ref || p === undefined || p >= i)) found.push(token) } if (found.length) result.set(node.id, found) }); return result }
function scalar(value: unknown) { return typeof value === 'string' ? JSON.stringify(value) : JSON.stringify(value) }
function yaml(value: unknown, indent = 0): string[] { const pad = ' '.repeat(indent); if (Array.isArray(value)) return value.flatMap(item => item && typeof item === 'object' ? [`${pad}-`, ...yaml(item, indent + 2)] : [`${pad}- ${scalar(item)}`]); if (value && typeof value === 'object') return Object.entries(value as Record<string, unknown>).flatMap(([key, item]) => item && typeof item === 'object' ? [`${pad}${key}:`, ...yaml(item, indent + 2)] : [`${pad}${key}: ${scalar(item)}`]); return [`${pad}${scalar(value)}`] }
const toYaml = (workflow: Workflow) => `${yaml(workflow).join('\n')}\n`

export default function WorkflowsPage() {
  const [items, setItems] = useState<Workflow[]>([]); const [selected, setSelected] = useState<Workflow | null>(null); const [agents, setAgents] = useState<Agent[]>([]); const [layout, setLayout] = useState<Layout>({}); const layoutRef = useRef<Layout>({}); const [activeId, setActiveId] = useState<string | null>(null); const [query, setQuery] = useState(''); const [dirty, setDirty] = useState(false); const [notice, setNotice] = useState(''); const [errors, setErrors] = useState<string[]>([]); const [checksText, setChecksText] = useState(''); const [checksError, setChecksError] = useState(''); const [tab, setTab] = useState<'overview'|'contracts'|'runs'|'alerts'>('overview'); const [inspector, setInspector] = useState(true); const [zoom, setZoom] = useState(1); const [pan, setPan] = useState({ x: 0, y: 0 }); const [edge, setEdge] = useState<string | null>(null); const [draftEdge, setDraftEdge] = useState<string | null>(null); const [objective, setObjective] = useState(''); const [run, setRun] = useState<RunState | null>(null); const [logs, setLogs] = useState<Log[]>([]); const [runs, setRuns] = useState<RunState[]>([]); const [eventMap, setEventMap] = useState<Record<string, RunEvent[]>>({}); const [busy, setBusy] = useState(false); const [resuming, setResuming] = useState(false); const drag = useRef<{ id: string; dx: number; dy: number } | null>(null); const panDrag = useRef<{ x: number; y: number; px: number; py: number } | null>(null); const controller = useRef<AbortController | null>(null); const canvasRef = useRef<HTMLDivElement>(null)
  const ordered = useMemo(() => selected ? chain(selected) : [], [selected]); const refs = useMemo(() => refErrors(ordered), [ordered]); const active = selected?.nodes.find(node => node.id === activeId); const filtered = items.filter(item => item.id.toLowerCase().includes(query.trim().toLowerCase())); const providers = [...new Set((selected?.nodes ?? []).filter(node => !isValidate(node)).map(node => agents.find(agent => agent.id === node.agent)?.provider).filter(Boolean))]; const interrupt = run?.interrupts?.find(item => item.status === 'pending') ?? null; const relevantRuns = active ? runs.filter(row => String(meta(row).workflow_id ?? '') === selected?.id) : []; const alerts = active ? relevantRuns.flatMap(row => (eventMap[row.run_id] ?? []).filter(event => event.node === active.id && (event.type === 'error' || event.type === 'validation_fail'))) : []
  useEffect(() => { void Promise.all([api<Workflow[]>('/api/workflows'), api<Agent[]>('/api/agents'), listRuns()]).then(([rows, listedAgents, recent]) => { setItems(rows); setSelected(rows[0] ?? null); setAgents(listedAgents); setRuns(recent) }).catch(error => setNotice(error instanceof ApiError ? error.message : 'Không thể tải dữ liệu')) }, [])
  useEffect(() => { if (!selected) return; setActiveId(null); setDirty(false); void api<{ nodes: Layout }>(`/api/workflows/${encodeURIComponent(selected.id)}/layout`).then(data => { const next = layoutFor(selected.nodes, data.nodes); layoutRef.current = next; setLayout(next) }).catch(() => { const next = layoutFor(selected.nodes, {}); layoutRef.current = next; setLayout(next) }).finally(() => requestAnimationFrame(fit)) }, [selected])
  useEffect(() => { if (active && isValidate(active)) setChecksText(JSON.stringify(active.checks, null, 2)); else setChecksText('') }, [activeId, selected])
  useEffect(() => () => controller.current?.abort(), [])
  useEffect(() => { if (!selected || (tab !== 'runs' && tab !== 'alerts')) return; const rows = runs.filter(row => String(meta(row).workflow_id ?? '') === selected.id); void Promise.all(rows.map(row => getRunEvents(row.run_id).then(events => [row.run_id, events] as const).catch(() => [row.run_id, []] as const))).then(entries => setEventMap(Object.fromEntries(entries))) }, [selected, runs, tab])
  const update = (fn: (workflow: Workflow) => Workflow) => { setSelected(current => current ? fn(current) : current); setDirty(true); setNotice(''); setErrors([]) }
  const validate = async () => { if (!selected) return false; const result = await validateWorkflow({ yaml_text: toYaml(selected) }); setErrors(result.errors); setNotice(result.ok ? 'Validate thành công.' : 'Workflow chưa hợp lệ — lỗi dưới đây đến từ backend.'); return result.ok }
  const save = async () => { if (!selected || checksError || !(await validate())) return false; try { const result = await api<Workflow>(`/api/workflows/${encodeURIComponent(selected.id)}/model`, { method: 'PUT', body: JSON.stringify({ model: selected }) }); await api(`/api/workflows/${encodeURIComponent(selected.id)}/layout`, { method: 'PUT', body: JSON.stringify({ nodes: layoutRef.current }) }); setSelected(result); setItems(rows => rows.map(row => row.id === result.id ? result : row)); setDirty(false); setNotice('Đã lưu workflow.'); return true } catch (error) { setNotice(error instanceof ApiError ? error.message : 'Lưu workflow thất bại'); return false } }
  const addNode = (agentId: string, point: { x: number; y: number }, type: 'agent'|'validate') => { if (!selected) return; const id = nextId(selected.nodes); const previous = ordered.at(-1); const node: Node = type === 'validate' ? { id, type, target: previous?.id ?? '', checks: [], on_fail: 'interrupt' } : { id, agent: agentId, prompt: previous ? `{{${previous.id}_output}}` : '{{objective}}', gate: 'none' }; update(workflow => ({ ...workflow, nodes: [...workflow.nodes, node], edges: previous ? [...workflow.edges, [previous.id, id]] : workflow.edges })); const next = { ...layoutRef.current, [id]: { x: Math.max(0, (point.x - pan.x) / zoom - 110), y: Math.max(0, (point.y - pan.y) / zoom - 63) } }; layoutRef.current = next; setLayout(next); setActiveId(id) }
  const connect = (from: string, to: string) => { if (from !== to) update(workflow => ({ ...workflow, edges: [...workflow.edges, [from, to]] })) }
  const removeEdge = () => { if (!selected || !edge) return; const [from, to] = edge.split('→'); update(workflow => ({ ...workflow, edges: workflow.edges.filter(item => !(item[0] === from && item[1] === to)) })); setEdge(null) }
  const saveLayout = () => { if (selected) void api(`/api/workflows/${encodeURIComponent(selected.id)}/layout`, { method: 'PUT', body: JSON.stringify({ nodes: layoutRef.current }) }).catch(() => undefined) }
  const stream = useCallback(async (response: Response) => { if (!response.body) throw new Error('Run stream unavailable'); for await (const item of parseSse(response.body)) { const data = item.data as Record<string, unknown>; setLogs(current => [{ ...data, type: item.event, ts: new Date().toISOString() } as Log, ...current].slice(0, 80)); if (item.event === 'state_snapshot' && data.state && typeof data.state === 'object') setRun(data.state as RunState); if (item.event === 'interrupt') setRun(current => current ? { ...current, status: 'interrupted', interrupts: [...(current.interrupts ?? []), data] } : current); if (item.event === 'done') setRun(current => current ? { ...current, status: String(data.status ?? current.status) } : current); if (item.event === 'error') setNotice(String(data.message ?? 'Run thất bại')) } }, [])
  const start = async () => { if (!selected || busy) return; if (!objective.trim()) { setNotice('Nhập mục tiêu trước khi chạy.'); return } setBusy(true); setLogs([]); setRun(null); try { if (dirty && !(await save())) return; const result = await validateWorkflow({ yaml_text: toYaml(selected) }); if (!result.ok) { setErrors(result.errors); setNotice('Workflow chưa hợp lệ — không thể chạy.'); return } controller.current = new AbortController(); await stream(await startRun(selected.id, objective.trim(), controller.current.signal)); void listRuns().then(setRuns).catch(() => undefined) } catch (error) { if ((error as Error).name !== 'AbortError') setNotice(error instanceof Error ? error.message : 'Run thất bại') } finally { setBusy(false); controller.current = null } }
  const gate = async (approved: boolean) => { if (!run || !interrupt) return; setResuming(true); try { await stream(await resumeGate(run.run_id, String(interrupt.interrupt_id), approved)) } catch (error) { setNotice(error instanceof Error ? error.message : 'Không thể tiếp tục run') } finally { setResuming(false) } }
  const nodeState = (id: string) => { const state = run ? meta(run) : {}; if (Array.isArray(state.completed_nodes) && state.completed_nodes.map(String).includes(id)) return 'done'; if (String(state.current_node ?? '') === id && busy) return 'running'; if (String(state.current_node ?? '') === id && run?.status === 'failed') return 'fail'; return 'idle' }
  // Single source of truth for screen→world conversion. Node drag used to mix
  // screen-pixel deltas with world-unit offsets (dividing by zoom twice in effect),
  // which drifted the node away from the cursor at any zoom other than 1 — worse
  // right after "Vừa khung", which almost never lands on exactly 100%.
  const toWorld = (clientX: number, clientY: number) => { const rect = canvasRef.current?.getBoundingClientRect(); if (!rect) return { x: 0, y: 0 }; return { x: (clientX - rect.left - pan.x) / zoom, y: (clientY - rect.top - pan.y) / zoom } }
  const panStart = (event: React.PointerEvent) => { if (event.target === event.currentTarget) panDrag.current = { x: pan.x, y: pan.y, px: event.clientX, py: event.clientY } }
  const pointerMove = (event: React.PointerEvent) => { if (drag.current) { const p = drag.current; const cursor = toWorld(event.clientX, event.clientY); const next = { ...layoutRef.current, [p.id]: { x: Math.max(0, cursor.x - p.dx), y: Math.max(0, cursor.y - p.dy) } }; layoutRef.current = next; setLayout(next) } else if (panDrag.current) { const p = panDrag.current; setPan({ x: p.x + event.clientX - p.px, y: p.y + event.clientY - p.py }) } }
  const pointerUp = () => { if (drag.current) saveLayout(); drag.current = null; panDrag.current = null }
  // Fits the view to the current node bounds, matching a floor of 0.5 so labels
  // stay legible — plain zoom(1)/pan(0,0) would leave nodes laid out below/right
  // of the canvas origin (e.g. code-task's "implement" at x:793,y:406) off-screen.
  const fit = () => {
    const positions = Object.values(layoutRef.current)
    const rect = canvasRef.current?.getBoundingClientRect()
    if (!positions.length || !rect || !rect.width || !rect.height) { setZoom(1); setPan({ x: 0, y: 0 }); return }
    const minX = Math.min(...positions.map(p => p.x)), minY = Math.min(...positions.map(p => p.y))
    const maxX = Math.max(...positions.map(p => p.x + size.width)), maxY = Math.max(...positions.map(p => p.y + size.height))
    const pad = 40
    const next = Math.max(.5, Math.min(1.8, Math.min((rect.width - pad * 2) / (maxX - minX), (rect.height - pad * 2) / (maxY - minY))))
    setZoom(Number(next.toFixed(2)))
    setPan({ x: Math.round((rect.width - (maxX - minX) * next) / 2 - minX * next), y: Math.round((rect.height - (maxY - minY) * next) / 2 - minY * next) })
  }
  const newWorkflow = () => { const draft: Workflow = { id: `workflow-${Date.now()}`, nodes: [], edges: [], stop: { max_nodes: 10, max_seconds: 3600 } }; setItems(rows => [...rows, draft]); setSelected(draft); setDirty(true); setNotice('Bản nháp mới — backend hiện không có endpoint tạo workflow.') }

  if (!selected) return <div className="p-6 text-sm text-muted">Không có workflow hợp lệ.</div>
  return <div className="flex h-full min-h-0 flex-col overflow-hidden bg-app">
    <header className="flex min-h-[var(--hub-size-toolbar)] flex-wrap items-center gap-3 border-b border-border-subtle bg-sidebar px-4 py-2"><div><div className="text-[10px] font-semibold uppercase tracking-[var(--hub-section-tracking)] text-muted">ĐIỀU PHỐI</div><div className="font-mono text-sm font-semibold">{selected.id}</div></div>{providers.length > 0 && <span className="rounded-full border border-border-subtle px-2 py-1 text-[10px] text-secondary">Nhà cung cấp: {providers.join(', ')}</span>}<span className="rounded-full border border-border-subtle px-2 py-1 text-[10px] text-muted">{busy ? 'Đang chạy' : run?.status ?? 'Sẵn sàng'}</span><div className="ml-auto flex flex-wrap items-center gap-2"><Input value={objective} onChange={event => setObjective(event.target.value)} placeholder="Mục tiêu chạy…" className="w-44" /><Button variant="primary" onClick={() => void start()} disabled={busy}>{busy ? 'Dừng' : 'Chạy'}</Button><Button variant="ghost" onClick={() => void validate()}>Validate</Button><Button variant="ghost" onClick={() => void save()}>Lưu</Button><Button variant="ghost" onClick={() => setInspector(value => !value)}>{inspector ? 'Ẩn Inspector' : 'Inspector'}</Button></div></header>
    {(notice || errors.length > 0) && <div className="mx-4 mt-3 rounded-lg border border-error bg-error-subtle p-3 text-xs text-error"><div>{notice || 'Workflow không hợp lệ.'}</div>{errors.length > 0 && <ul className="mt-2 list-disc space-y-1 pl-5">{errors.map((item, index) => <li key={`${item}-${index}`}>{item}</li>)}</ul>}</div>}
    <div className={`grid min-h-0 flex-1 ${inspector ? 'grid-cols-[248px_minmax(0,1fr)_320px]' : 'grid-cols-[248px_minmax(0,1fr)]'}`}><Sidebar items={filtered} selected={selected} query={query} setQuery={setQuery} onSelect={setSelected} onNew={newWorkflow} agents={agents} />
      <main className="flex min-h-0 min-w-0 flex-col"><div className="flex items-center gap-2 border-b border-border-subtle px-4 py-2 text-xs text-secondary"><span>{selected.nodes.length} node · {selected.edges.length} kết nối</span><span>{dirty ? '● Chưa lưu' : 'Đã đồng bộ'}</span><span className="ml-auto"><Button variant="ghost" size="sm" onClick={() => setZoom(value => Math.min(1.8, value + .1))}>＋</Button><Button variant="ghost" size="sm" onClick={() => setZoom(value => Math.max(.5, value - .1))}>−</Button><Button variant="ghost" size="sm" onClick={fit}>Vừa khung</Button>{edge && <Button variant="destructive" size="sm" onClick={removeEdge}>Xóa cạnh</Button>}</span></div><div className="grid min-h-0 flex-1 grid-rows-[minmax(360px,1fr)_minmax(150px,30%)]"><section ref={canvasRef} className="relative min-h-0 overflow-hidden border-b border-border-subtle" onPointerDown={panStart} onPointerMove={pointerMove} onPointerUp={pointerUp} onPointerLeave={pointerUp} onDragOver={event => event.preventDefault()} onDrop={event => { const rect = event.currentTarget.getBoundingClientRect(); const type = event.dataTransfer.getData('type'); const agent = event.dataTransfer.getData('agent'); if (type === 'validate') addNode('', { x: event.clientX - rect.left, y: event.clientY - rect.top }, 'validate'); else if (agent) addNode(agent, { x: event.clientX - rect.left, y: event.clientY - rect.top }, 'agent') }} style={{ backgroundImage: 'radial-gradient(var(--hub-border-strong) 1px, transparent 1px)', backgroundSize: '20px 20px' }}><Canvas workflow={selected} layout={layout} zoom={zoom} pan={pan} activeId={activeId} selectedEdge={edge} draftEdge={draftEdge} state={nodeState} onSelect={setActiveId} onDrag={value => { const cursor = toWorld(value.clientX, value.clientY); drag.current = { id: value.id, dx: cursor.x - value.nodeX, dy: cursor.y - value.nodeY } }} onEdgeSelect={setEdge} onEdgeStart={setDraftEdge} onConnect={connect} onEdgeEnd={() => setDraftEdge(null)} /></section><RunLog logs={logs} run={run} interrupt={interrupt} onGate={gate} resuming={resuming} /></div></main>
      {inspector && <Inspector node={active} nodes={ordered} agents={agents} errors={active ? refs.get(active.id) ?? [] : []} tab={tab} setTab={setTab} runs={relevantRuns} alerts={alerts} checksText={checksText} setChecksText={setChecksText} setChecksError={setChecksError} checksError={checksError} update={update} deleteNode={() => { if (!active) return; update(workflow => ({ ...workflow, nodes: workflow.nodes.filter(node => node.id !== active.id), edges: workflow.edges.filter(item => item[0] !== active.id && item[1] !== active.id) })); setActiveId(null) }} />}</div>
  </div>
}

function Sidebar({ items, selected, query, setQuery, onSelect, onNew, agents }: { items: Workflow[]; selected: Workflow; query: string; setQuery: (value: string) => void; onSelect: (workflow: Workflow) => void; onNew: () => void; agents: Agent[] }) { return <aside className="flex min-h-0 flex-col overflow-y-auto border-r border-border-subtle bg-sidebar p-3"><div className="mb-3 flex items-center justify-between"><span className="text-[10px] font-semibold uppercase tracking-[var(--hub-section-tracking)] text-muted">Workflows</span><Button variant="ghost" size="sm" onClick={onNew}>+ Mới</Button></div><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Tìm workflow…" className="mb-3" />{items.map(item => <Button key={item.id} variant="ghost" selected={item.id === selected.id} onClick={() => onSelect(item)} className="mb-1 w-full justify-start px-3 py-2 text-left"><span><span className="block font-mono text-xs">{item.id}</span><span className="text-[10px] text-secondary">{item.nodes.length} node · {item.edges.length} cạnh</span></span></Button>)}<div className="mt-4 border-t border-border-subtle pt-3"><div className="mb-2 text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted">Palette</div>{agents.map(agent => <div key={agent.id} draggable onDragStart={event => event.dataTransfer.setData('agent', agent.id)} className="mb-2 cursor-grab rounded-md border border-border-subtle bg-surface px-3 py-2 text-xs">● {agent.name ?? agent.id}</div>)}<div draggable onDragStart={event => event.dataTransfer.setData('type', 'validate')} className="cursor-grab rounded-md border border-[var(--hub-node-validate)] bg-surface px-3 py-2 text-xs text-[var(--hub-node-validate)]">◇ validate</div></div></aside> }

function Canvas({ workflow, layout, zoom, pan, activeId, selectedEdge, draftEdge, state, onSelect, onDrag, onEdgeSelect, onEdgeStart, onConnect, onEdgeEnd }: { workflow: Workflow; layout: Layout; zoom: number; pan: { x: number; y: number }; activeId: string | null; selectedEdge: string | null; draftEdge: string | null; state: (id: string) => string; onSelect: (id: string) => void; onDrag: (value: { id: string; clientX: number; clientY: number; nodeX: number; nodeY: number }) => void; onEdgeSelect: (edge: string) => void; onEdgeStart: (id: string) => void; onConnect: (from: string, to: string) => void; onEdgeEnd: () => void }) { return <div className="absolute inset-0" style={{ transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom})`, transformOrigin: '0 0' }}><svg className="pointer-events-none absolute left-0 top-0 h-[1600px] w-[2400px] overflow-visible"><defs><marker id="workflow-arrow" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill="var(--hub-edge-normal)" /></marker></defs>{workflow.edges.map(([from, to]) => { const a = layout[from], b = layout[to]; if (!a || !b) return null; const key = `${from}→${to}`; const x1 = a.x + size.width, y1 = a.y + size.height / 2, x2 = b.x, y2 = b.y + size.height / 2, cx = (x1 + x2) / 2; const path = `M ${x1} ${y1} C ${cx} ${y1}, ${cx} ${y2}, ${x2} ${y2}`; return <g key={key}><path d={path} fill="none" stroke={key === selectedEdge ? 'var(--hub-edge-selected)' : 'var(--hub-edge-normal)'} strokeWidth={key === selectedEdge ? 3 : 2} markerEnd="url(#workflow-arrow)" /><path d={path} fill="none" stroke="transparent" strokeWidth="14" className="pointer-events-auto cursor-pointer" onPointerDown={event => { event.stopPropagation(); onEdgeSelect(key) }} /></g> })}</svg>{workflow.nodes.map(node => { const p = layout[node.id] ?? { x: 40, y: 60 }; const status = state(node.id); return <div key={node.id} style={{ left: p.x, top: p.y, width: size.width }} onPointerDown={event => { if ((event.target as HTMLElement).dataset.port) return; event.stopPropagation(); onDrag({ id: node.id, clientX: event.clientX, clientY: event.clientY, nodeX: p.x, nodeY: p.y }); onSelect(node.id) }} className={`absolute select-none rounded-xl border bg-surface p-3 shadow-lg ${activeId === node.id ? 'border-[var(--hub-accent)] ring-1 ring-[var(--hub-accent)]' : isValidate(node) ? 'border-[var(--hub-node-validate)]' : 'border-border-subtle'} ${status === 'running' ? 'node-pulse border-[var(--hub-accent)]' : ''}`}><div className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-md bg-accent/15 text-[var(--hub-node-agent)]">{isValidate(node) ? '◇' : '✦'}</span><span className="truncate font-mono text-xs font-semibold">{node.id}</span>{!isValidate(node) && node.gate === 'approval' && <span className="ml-auto rounded-full border border-warning px-1 text-[9px] text-warning">approval</span>}</div><div className="mt-3 truncate text-[10px] text-secondary">{isValidate(node) ? `validate · ${node.target || 'chưa chọn target'}` : node.agent}</div><div className="mt-3 flex justify-between text-[10px] text-muted"><span>{status === 'running' ? 'Đang chạy…' : status === 'done' ? 'Hoàn tất' : status === 'fail' ? 'Lỗi' : isValidate(node) ? `${node.checks.length} checks` : 'agent'}</span><span>{status === 'done' ? '✓' : status === 'fail' ? '!' : status === 'running' ? '●' : '○'}</span></div><IconButton data-port="right" icon={<span className="h-2 w-2 rounded-full bg-[var(--hub-edge-normal)]" />} onPointerDown={event => { event.stopPropagation(); onEdgeStart(node.id) }} variant="handle" className="absolute -right-2 top-1/2 -translate-y-1/2" aria-label={`Nối từ ${node.id}`} /><IconButton data-port="left" icon={<span className="h-2 w-2 rounded-full bg-[var(--hub-edge-normal)]" />} onPointerUp={event => { event.stopPropagation(); if (draftEdge) onConnect(draftEdge, node.id); onEdgeEnd() }} variant="handle" className="absolute -left-2 top-1/2 -translate-y-1/2" aria-label={`Nối vào ${node.id}`} /></div> })}</div> }

function RunLog({ logs, run, interrupt, onGate, resuming }: { logs: Log[]; run: RunState | null; interrupt: Record<string, unknown> | null; onGate: (approved: boolean) => void; resuming: boolean }) { return <section className="min-h-0 overflow-y-auto bg-surface p-3"><div className="mb-2 flex justify-between text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted"><span>Run Log</span><span>{run ? `${run.run_id} · ${run.status}` : 'Chưa có run'}</span></div>{interrupt && <GateCard interrupt={interrupt} onChoice={onGate} resuming={resuming} />}{logs.length === 0 ? <div className="rounded-md border border-dashed border-border-subtle p-4 text-xs text-muted">Log nhận event thật từ SSE khi workflow chạy.</div> : logs.map((item, index) => <div key={`${item.ts}-${index}`} className="grid grid-cols-[70px_1fr_auto] gap-3 border-b border-border-subtle py-2 text-[10px]"><span className="font-mono text-muted">{new Date(item.ts).toLocaleTimeString('vi-VN')}</span><span><b>{String(item.node ?? 'workflow')}</b> · {item.type ?? 'event'} {item.text ? String(item.text).slice(0, 120) : ''}</span><span className={item.type === 'error' || item.type === 'validation_fail' ? 'text-error' : item.type === 'done' ? 'text-success' : 'text-[var(--hub-accent)]'}>{item.type === 'error' || item.type === 'validation_fail' ? 'fail' : item.type === 'done' ? 'pass' : 'live'}</span></div>)}</section> }

function Inspector({ node, nodes, agents, errors, tab, setTab, runs, alerts, checksText, setChecksText, setChecksError, checksError, update, deleteNode }: { node?: Node; nodes: Node[]; agents: Agent[]; errors: string[]; tab: 'overview'|'contracts'|'runs'|'alerts'; setTab: (tab: 'overview'|'contracts'|'runs'|'alerts') => void; runs: RunState[]; alerts: RunEvent[]; checksText: string; setChecksText: (value: string) => void; setChecksError: (value: string) => void; checksError: string; update: (fn: (workflow: Workflow) => Workflow) => void; deleteNode: () => void }) { if (!node) return <aside className="border-l border-border-subtle bg-elevated p-4 text-xs text-muted">Chọn node để mở Inspector.</aside>; const prior = nodes.slice(0, nodes.findIndex(item => item.id === node.id)); const change = (field: string, value: unknown) => update(workflow => ({ ...workflow, nodes: workflow.nodes.map(item => item.id === node.id ? { ...item, [field]: value } as Node : item) })); const tokens = !isValidate(node) ? [...node.prompt.matchAll(/\{\{(.*?)\}\}/g)].map(match => match[1].trim()) : []; return <aside className="flex min-h-0 flex-col border-l border-border-subtle bg-elevated"><div className="flex justify-between border-b border-border-subtle p-4"><div><div className="text-[10px] uppercase tracking-[var(--hub-section-tracking)] text-muted">Inspector</div><div className="font-mono text-sm">{node.id}</div></div><Button variant="ghost" size="sm" onClick={deleteNode}>Xóa</Button></div><div className="grid grid-cols-4 border-b border-border-subtle px-2">{(['overview','contracts','runs','alerts'] as const).map(item => <button key={item} onClick={() => setTab(item)} className={`border-b-2 py-3 text-[10px] ${tab === item ? 'border-[var(--hub-accent)] text-primary' : 'border-transparent text-muted'}`}>{item === 'overview' ? 'Tổng quan' : item === 'contracts' ? 'Hợp đồng' : item === 'runs' ? 'Runs' : 'Cảnh báo'}</button>)}</div><div className="min-h-0 flex-1 overflow-y-auto p-4">{tab === 'overview' && <Overview node={node} prior={prior} agents={agents} checksText={checksText} setChecksText={setChecksText} setChecksError={setChecksError} checksError={checksError} change={change} update={update} errors={errors} />}{tab === 'contracts' && <div className="space-y-2 text-xs">{isValidate(node) ? <pre className="overflow-auto rounded-md bg-app p-3 font-mono text-[10px]">{JSON.stringify({ target: node.target, checks: node.checks }, null, 2)}</pre> : tokens.length ? tokens.map(token => <div key={token} className="rounded-md border border-border-subtle bg-surface p-2 font-mono text-[10px]">{`{{${token}}}`} → {token === 'objective' ? 'mục tiêu run' : token.endsWith('_output') ? `output node ${token.slice(0, -7)}` : `claims node ${token.slice(0, -7)}`}</div>) : <span className="text-muted">Không có token.</span>}</div>}{tab === 'runs' && <div className="space-y-2 text-[10px]">{runs.length ? runs.map(item => <div key={item.run_id} className="rounded-md border border-border-subtle bg-surface p-2 font-mono">{item.run_id} · {item.status} · {String(meta(item).current_node ?? '')}</div>) : <span className="text-muted">Chưa có run thật.</span>}</div>}{tab === 'alerts' && <div className="space-y-2 text-[10px]">{alerts.length ? alerts.map((item, index) => <div key={`${item.ts}-${index}`} className="rounded-md border border-error bg-error-subtle p-2 text-error">{item.type}: {String(item.message ?? item.target ?? 'Sự kiện lỗi')}</div>) : <span className="text-muted">Không có cảnh báo thật.</span>}</div>}</div></aside> }
// @ts-ignore update is retained in the shared inspector contract for editing symmetry.
function Overview({ node, prior, agents, checksText, setChecksText, setChecksError, checksError, change, update, errors }: { node: Node; prior: Node[]; agents: Agent[]; checksText: string; setChecksText: (value: string) => void; setChecksError: (value: string) => void; checksError: string; change: (field: string, value: unknown) => void; update: (fn: (workflow: Workflow) => Workflow) => void; errors: string[] }) { return <>{isValidate(node) ? <><label className="mb-3 block text-[10px] text-muted">target<Select value={node.target} onChange={event => change('target', event.target.value)}><option value="">Chọn node trước</option>{prior.map(item => <option key={item.id} value={item.id}>{item.id}</option>)}</Select></label><label className="mb-3 block text-[10px] text-muted">on_fail<Select value={node.on_fail} onChange={event => change('on_fail', event.target.value)}><option value="interrupt">interrupt</option><option value="fail">fail</option></Select></label><label className="block text-[10px] text-muted">checks JSON<Textarea value={checksText} onChange={event => { setChecksText(event.target.value); try { const value = JSON.parse(event.target.value); if (!Array.isArray(value)) throw new Error(); setChecksError(''); change('checks', value) } catch { setChecksError('JSON không hợp lệ') } }} rows={8} />{checksError && <span className="text-error">{checksError}</span>}</label></> : <><label className="mb-3 block text-[10px] text-muted">agent<Select value={node.agent} onChange={event => change('agent', event.target.value)}>{agents.map(agent => <option key={agent.id} value={agent.id}>{agent.id}</option>)}</Select></label><label className="mb-3 block text-[10px] text-muted">gate<Select value={node.gate} onChange={event => change('gate', event.target.value)}><option value="none">none</option><option value="approval">approval</option></Select></label><label className="block text-[10px] text-muted">prompt<Textarea value={node.prompt} onChange={event => change('prompt', event.target.value)} rows={8} /></label></>}{errors.length > 0 && <div className="mt-3 rounded-md border border-error bg-error-subtle p-2 text-[10px] text-error">Tham chiếu không hợp lệ: {errors.join(', ')}</div>}</> }
